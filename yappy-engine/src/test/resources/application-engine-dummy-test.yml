micronaut:
  application:
    name: yappy-engine-test
  config-client:
    enabled: false

grpc:
  server:
    enabled: false
  client:
    plaintext: true

local:
  services:
    ports:
      dummy-service: 50061

kafka:
  enabled: true

app:
  config:
    cluster-name: engineDummyProcessorTestCluster
  engine:
    bootstrapper:
      enabled: false