# Consul client configuration
consul:
  client:
    enabled: true
    config:
      enabled: true
      format: properties
      path: config/pipeline
    registration:
      enabled: true
    defaultZone: "${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"