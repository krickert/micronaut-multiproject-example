micronaut:
  application:
    name: test-application

app:
  config:
    cluster-name: test-cluster


grpc.server:
  enabled: true
  port: ${random.port}

# Consul client configuration
consul:
  client:
    enabled: true
    config:
      enabled: true
      format: properties
      path: config/pipeline
