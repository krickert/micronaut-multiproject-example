# Enable test containers
testcontainers.enabled=true
testcontainers.consul=true

# KV properties to set on startup (optional)
# containers.hashicorp-consul.kv-properties[0]=configs/test-key=test-value

# Consul test configuration - fallback values
consul.client.host=localhost
consul.client.port=8500
consul.client.default-zone=test
consul.discovery.host=localhost
consul.discovery.port=8500