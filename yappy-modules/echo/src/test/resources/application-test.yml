micronaut:
  application:
    name: echo
grpc:
  server:
    port: ${random.port}
  client:
    plaintext: true
    discovery:
      enabled: true
consul:
  client:
    registration:
      enabled: true
      check:
        enabled: true
