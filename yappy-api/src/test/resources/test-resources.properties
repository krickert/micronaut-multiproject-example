# Test resources configuration for yappy-api
# Triggers test resources when needed by tests

# Kafka test resource
kafka.bootstrap.servers=

# Consul test resource (if needed for integration tests)
# consul.client.host=
# consul.client.port=