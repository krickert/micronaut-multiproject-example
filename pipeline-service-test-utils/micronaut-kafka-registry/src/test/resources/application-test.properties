# Define the topic names for Kafka integration tests
kafka.topic.pipedoc=test-pipedoc
kafka.topic.pipedoc-apicurio=test-pipedoc-apicurio
# Schema registry configuration
schema.registry.enabled=true
kafka.enabled=true
consul.client.watch.enabled=false
consul.client.discovery.enabled=false
consul.client.config.enabled=false
micronaut.caches.discovery-client.enabled=false
consul.client.registration.enabled=false

# Schema registry configuration
schema.registry.type=apicurio