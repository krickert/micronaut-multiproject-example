# Service Discovery Test Properties

# Application configuration
micronaut.application.name=service-discovery-test

# Enable service discovery
micronaut.discovery.enabled=true

# Enable config client
micronaut.config-client.enabled=true

# Consul client configuration
consul.client.registration.enabled=true
consul.client.config.format=YAML
consul.client.config.path=/config
consul.client.watch.enabled=true

# Logging configuration
logging.level.com.krickert=DEBUG
